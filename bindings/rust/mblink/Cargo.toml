[package]
name = "mblink"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Safe Rust bindings for MBlink"

[dependencies]
mblink-sys = { path = "../mblink-sys" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
