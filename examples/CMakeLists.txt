# LightUI Examples CMake Configuration

cmake_minimum_required(VERSION 3.15)

# Example applications will be added here as they are implemented
# For now, this file is a placeholder

message(STATUS "Examples directory configured (no examples built yet)")

# TODO: Add example applications
# Example structure:
# add_executable(hello_world hello_world/main.cpp)
# target_link_libraries(hello_world PRIVATE lightui_core)

